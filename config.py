# --- НАСТРОЙКИ ПУТИ ---
SPRITE_DIR = 'assets'

# --- НОВІ НАСТРОЙКИ ДЛЯ ПРИВІТАННЯ ---
WELCOME_SPRITE = 'welcome/hello_01.png' # <<< Шлях до спрайту привітання (приклад)
WELCOME_DURATION_MS = 3000 # Тривалість показу привітання: 3 секунди
# ------------------------------------

# --- НАСТРОЙКИ АНИМАЦИИ ---

# 1. IDLE: ОДИН статичний спрайт
IDLE_SPRITES = ['idle/idle_01.png'] 
ANIMATION_SPEED_MS = 10000 # Дуже повільна швидкість (10 сек), щоб animation_timer не спрацьовував у статичному режимі

# 4 кадра для анимации ходьбы (WALK)
WALK_SPRITES_RIGHT = [f'walk_right/walk_right_0{i}.png' for i in range(1, 5)] 

# НОВИЙ КОД: 4 кадра для анимации ходьбы влево
# Якщо ви назвали їх, наприклад, walk_left_01.png
WALK_SPRITES_LEFT = [f'walk_left/walk_left_0{i}.png' for i in range(1, 5)]

# --- НАСТРОЙКИ МОРГАНИЯ ---

# 2. BLINK: 7 кадрів для моргання (шляхи тепер відносно папки 'assets/blink/')
BLINK_SPRITES = [f'blink/blink_0{i}.png' for i in range(1, 8)] # <<< ЗМІНА ШЛЯХУ
BLINK_FRAME_DURATION_MS = 30 # Ускоряем кадры моргания для быстрого эффекта
BLINK_INTERVAL_MIN_SEC = 5 # Мінімальний інтервал моргання (секунди)
BLINK_INTERVAL_MAX_SEC = 15 # Максимальний інтервал моргання (секунди)

# --- НАСТРОЙКИ ЛОГИКИ ПЕРЕМЕЩЕНИЯ ---

WALK_DECISION_INTERVAL_MS = 15000 # Помощник решает, идти ли, каждые 15 секунд
WALK_PIXELS_PER_STEP = 2 # Скорость ходьбы (2 пикселя за шаг)
WALK_MAX_DISTANCE = 300 # Максимальное расстояние для случайной точки (пикселей)

# 🌟 НОВІ НАЛАШТУВАННЯ ДЛЯ ПРЕСЕТІВ 🌟
PRESET_FILE = "presets.json" # Файл для збереження пресетів

# ⚠️ УВАГА: Для запуску програм з певним розміром/позицією потрібна стороння бібліотека
# наприклад, 'pygetwindow' (Windows/Linux) або 'pyautogui' (Mac), але для спрощення
# ми реалізуємо функціонал тільки для відкриття, а логіку зміни розміру залиште
# як заглушку, поки не буде додано відповідну кросплатформну бібліотеку.

# --- НАСТРОЙКИ ПРОДУКТИВНОСТИ ---

HABIT_REMINDER_MINUTES = 60 # Напоминание о разминке каждый час

